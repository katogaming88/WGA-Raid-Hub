# Row-Level Security Policy Reference

This file documents the RLS policies on every public table. The generated schema docs in [`dbdoc/`](../dbdoc/README.md) cover structure (columns, constraints, indexes, FKs) but tbls does not introspect policies, so this file fills that gap by hand.

**Maintenance contract:** policies change only through files in `supabase/migrations/`. Any migration that creates, alters, or drops a policy must update this file in the same PR. The schema-docs CI workflow fails a PR that touches policy SQL without touching this file.

## Concepts

- **`my_team_role(team_id)`**: returns the calling user's role on the given team, resolved from `team_members` via `auth.uid()`. The role column allows exactly `raider`, `officer`, or `team_leader` (top tier within one team; named by [#294](https://github.com/katogaming88/WGA-Raid-Hub/issues/294) to stay distinct from site admins). Officer-gated policies accept `officer` or `team_leader`; team-leader-gated policies accept `team_leader` only. No policy grants a `raider` role any team data, though a member of any role can read their own `team_members` row through the self-read policy ([#212](https://github.com/katogaming88/WGA-Raid-Hub/issues/212), see the matrix note); being on a team's roster otherwise grants no access beyond public read.
- **`is_site_admin()`**: true when `auth.uid()` appears in `site_admins`. This is a separate, global mechanism, not a `team_members` role: site admins pass every policy that ORs it in ("+site" in the matrix) on every team.
- **`is_guild_officer()`**: true when `auth.uid()` appears in `guild_officers` ([#607](https://github.com/katogaming88/WGA-Raid-Hub/issues/607)). Structurally identical to `is_site_admin()` -- a standalone table, granted/revoked manually by a site admin (`admin.html`'s Guild Officers tab), not a `team_members` role or derived from holding `officer`/`team_leader` on some other team. Narrower than site admin: OR'd into only `players`/`attendance` write, `audit_log`/`team_members` read, and the `write_audit_log()`/`set_guild_officer_bios()` function gates ("+guild" in the matrix). Deliberately never OR'd into approvals, season settings, priority generation, or loot import -- a guild officer is denied there exactly like a raider.
- **Public read**: `FOR SELECT USING (true)`. The site's public pages (roster, loot feed, standings) read these tables anonymously.
- **`claude_readers`**: nologin group role for read-only AI/analysis access. Holds one `Claude readers read <table>` SELECT policy on every public table, scoped `TO claude_readers` only. Created by `supabase/roles.sql`; see [claude-readonly-db-access.md](claude-readonly-db-access.md). Add a matching policy when adding a table.
- **Service role**: Supabase's `service_role` bypasses RLS entirely. Tables listed below with no write policy are writable only through the service role (or direct SQL as `postgres`).

## Per-table policy matrix

### How to read this matrix

RLS is deny-by-default: with RLS enabled (it is, on all 43 tables), nobody can touch any row unless a policy explicitly grants it. Policies are additive; if any one policy matches an actor and operation, the action is allowed. Each row below summarizes which grants exist for that table.

- **Public SELECT**: "yes" means a `FOR SELECT USING (true)` policy exists, so anyone (including anonymous visitors) can read every row. This is how the public site serves roster, loot, and standings without login. "no" means there is no public read path.
- **Officer**: what a team officer can do, scoped to their own team's rows via `my_team_role(team_id)`. "all ops" covers SELECT, INSERT, UPDATE, and DELETE. "SELECT, UPDATE" means they can see and modify existing rows but cannot insert or delete. Team leaders pass every officer check too, since these policies accept both roles.
- **Team leader**: grants beyond the officer policies. "(via officer)" means no separate team-leader policy exists; team leaders qualify under the Officer column. "all ops +site" marks team-leader-only write policies that officers do NOT pass. "+site" anywhere means site admins are OR'd in via `is_site_admin()` regardless of team.
- **Notes**: exceptions and known gaps.
- **A blank cell** means no policy grants that actor anything, so deny-by-default applies. A table with only Public SELECT (like `classes_specs` or `teams`) is a read-only lookup: everyone can read it and only the service role can write it. A table blank in every column except Notes (`site_admins`) is invisible to everyone but the actor named there.

One thing the matrix hides on purpose: every table also carries a `claude_readers` SELECT policy (uniform across all 43 tables, so it is stated here instead of as a column).

| Table | Public SELECT | Officer | Team leader | Notes |
| --- | --- | --- | --- | --- |
| attendance | yes | all ops +site | (via officer) | +guild ([#607](https://github.com/katogaming88/WGA-Raid-Hub/issues/607)): `is_guild_officer()` OR'd into the write policy, full access on every team. `is_site_admin()` OR'd in too, same as every other officer-write policy below |
| audit_log | no | SELECT +site | | +guild ([#607](https://github.com/katogaming88/WGA-Raid-Hub/issues/607)), read-only. No INSERT policy; only write path is `write_audit_log()` (SECURITY DEFINER, officer/team_leader/site_admin/guild_officer/boe_manager, [#214](https://github.com/katogaming88/WGA-Raid-Hub/issues/214)) |
| bis_items | yes | all ops +site | (via officer) | Team resolved through `players.team_id` subquery. A raider can also UPDATE `obtained` on their own rows via `is_own_player(player_id)` ([#651](https://github.com/katogaming88/WGA-Raid-Hub/issues/651), Raider.IO tier sync) -- a trigger (`restrict_bis_items_update_to_obtained`) locks every UPDATE, officer or raider, to that one column, since `item_id`/`player_id`/`slot`/`season` are only ever set at insert |
| bis_requests | no | SELECT +site, UPDATE +site | | No table INSERT policy; `submit_bis_link()` (SECURITY DEFINER) is the only write path ([#404](https://github.com/katogaming88/WGA-Raid-Hub/issues/404)). Deliberately no `is_guild_officer()` grant -- approvals stay excluded from the guild officer tier ([#607](https://github.com/katogaming88/WGA-Raid-Hub/issues/607)) |
| boe_items | no | SELECT +site | | No public read (money data: payouts owed per person and live listing prices are undercutting intel, the `item_preferences` privacy call). No table INSERT policy; `submit_boe_found()` (SECURITY DEFINER, anon+authenticated) is the only insert path ([#745](https://github.com/katogaming88/WGA-Raid-Hub/issues/745)). SELECT is the caller's own team for an officer, plus `is_boe_manager()` for every team, so a manager can see every row they may mutate. UPDATE/DELETE gated on `is_boe_manager()` or `is_site_admin()` -- a `boe_managers` grant, not plain officer role -- and a plain UPDATE is metadata-only (note, finder, item, track, upgrade rank, season): a `check_boe_status_transition` trigger blocks status/money/timestamp edits, so lifecycle changes go through the RPCs. That trigger is an allow-list by subtraction, so `found_posted_at` ([#956](https://github.com/katogaming88/WGA-Raid-Hub/issues/956)) is service-role-only the day it exists and needed no policy of its own: `boe-webhook` claims the row with it before posting, so a find is announced once and a refused post clears it again. Two of those admit a team officer or leader for the row's own team through `can_settle_boe(team_id)`: `boe_mark_paid` and the paid-to-sold edge of `boe_revert` ([#888](https://github.com/katogaming88/WGA-Raid-Hub/issues/888)); listing, sale, retire, edit and delete stay with the grant. A raider reads their own rows via `is_own_player(player_id)` or, since [#889](https://github.com/katogaming88/WGA-Raid-Hub/issues/889), the Discord account they submitted from (`finder_discord_id = current_discord_id()`, both sides guarded non-null so a signed-out find never matches an account with none). Deliberately no `is_guild_officer()` grant, like approvals |
| boe_listings | no | SELECT +site | | One row per AH listing event so relists keep their history ([#745](https://github.com/katogaming88/WGA-Raid-Hub/issues/745)). SELECT for an officer on that team, any BoE manager, or a site admin, and for a raider on the listings of a `boe_items` row they can read ([#889](https://github.com/katogaming88/WGA-Raid-Hub/issues/889); the policy's subquery runs under `boe_items`' own policies, so it admits exactly those); DELETE gated on `is_boe_manager()`/`is_site_admin()`. No INSERT policy; `boe_record_listing()` (SECURITY DEFINER) is the only insert path |
| boe_managers | no | SELECT +site | | The standalone grant that gates BoE money mutations, same shape as `guild_officers` and `site_admins` ([#745](https://github.com/katogaming88/WGA-Raid-Hub/issues/745), reshaped guild-wide in [#766](https://github.com/katogaming88/WGA-Raid-Hub/issues/766)). `discord_id` + `auth_user_id`, no team column: BoEs are guild property, so a grantee is authorized everywhere and stays so until a site admin revokes it. Site admins assign (all ops via `is_site_admin()`, through the `admin_*_boe_manager` trio since there is no direct write grant); SELECT for any officer or team leader on any team via `is_any_team_officer()`. That read is deliberately wider than `guild_officers`' site-admin-only one, so an ungranted officer looking at a find they cannot act on can see who can |
| classes_specs | yes | | | Read-only lookup; no write policy |
| guild_officers | no | | | Guild officers only ([#607](https://github.com/katogaming88/WGA-Raid-Hub/issues/607)): SELECT and all ops via `is_site_admin()`, mirrors `site_admins` |
| item_bosses | yes | | | Read-only lookup; no write policy |
| item_preferences | no | SELECT +site, UPDATE +site | (via officer) | +guild on SELECT only (view-only, same as `audit_log`/`team_members`; `is_guild_officer()` is not OR'd into the officer-only UPDATE, `is_site_admin()` is). Raider wishlist tags ([#515](https://github.com/katogaming88/WGA-Raid-Hub/issues/515)). No public read, unlike `bis_items` -- these tags are more personal/opinionated. A raider also INSERT/UPDATE/DELETEs their own rows via `is_own_player(player_id)`, same self-service predicate as `streamers`/`notifications`. The officer UPDATE grant is narrower than it looks: a trigger (`restrict_item_preferences_officer_update_to_note_clear()`) restricts any non-owning-player update to setting `note` to `NULL` only -- an officer can clear a raider's note (e.g. redundant/noisy text) but cannot rewrite it or touch `status`/`item_id`/`slot` |
| items | yes | | | Read-only lookup; no write policy |
| mplus_exclusion_requests | no | SELECT +site, UPDATE +site | | No table INSERT policy; `submit_mplus_exclusion()` (SECURITY DEFINER) is the only write path ([#405](https://github.com/katogaming88/WGA-Raid-Hub/issues/405)) |
| no_character_dismissals | no | | | Global, not per-team, no officer-specific policy at all ([#512](https://github.com/katogaming88/WGA-Raid-Hub/issues/512)). A raider manages their own row via a direct `auth_user_id = auth.uid()` check (no helper predicate needed, single-column) -- permanent "I don't have a character yet" opt-out for the claim prompt |
| notifications | no | | | No table INSERT policy for anyone, including officers; `notify_player()` (SECURITY DEFINER) is the only write path ([#151](https://github.com/katogaming88/WGA-Raid-Hub/issues/151)). A raider reads and marks-read their own rows via `is_own_player(player_id)`, same self-service predicate as `streamers` |
| player_equipped_gear | yes | all ops +site | (via officer) | One row per (player, Blizzard API equipment slot), synced from the Blizzard Character Equipment Summary endpoint by the `blizzard-gear-sync` Edge Function -- either its officer-triggered, JWT-forwarded on-demand path, or its scheduled cron sweep (service role, bypasses RLS). Team resolved through `players.team_id` subquery, like `bis_items`. Feeds `generate_priority_order()`'s equipped-item-level fairness factor |
| player_officer_notes | no | all ops +site | (via officer) | Officer-only annotations on a roster slot ([#925](https://github.com/katogaming88/WGA-Raid-Hub/issues/925)): the private officer note, and why a player was removed plus the freeform specifics. One row per `players` row, created on first write, team resolved by its own `team_id` column with the `check_team_id_matches_player` guard. No public read on purpose: these lived on `players` until #925, where its `qual = true` public policy and table-level `anon` grant made them readable with the publishable key, and its `polroles` of PUBLIC made them readable by every signed-in raider too. `archive_player()` is the only writer of the two archive columns |
| player_wcl_season_perf | yes | all ops +site | (via officer) | |
| players | yes | all ops +site | (via officer) | +guild ([#607](https://github.com/katogaming88/WGA-Raid-Hub/issues/607)): `is_guild_officer()` OR'd into the write policy, full access on every team. `is_site_admin()` OR'd in too. `officer_notes`, `archived_reason` and `archived_reason_detail` moved off this table to `player_officer_notes` in [#925](https://github.com/katogaming88/WGA-Raid-Hub/issues/925), because the public read policy below exposed them; `m_plus_note` stayed, since the public profile renders it. A raider can also UPDATE `bonus_roll_encounter_id` on their own row via `is_own_player(id)` (Bonus Roll target) -- a trigger (`restrict_players_self_update_to_bonus_roll`) locks every non-officer UPDATE to that one column, comparing the full row via `to_jsonb()` rather than an explicit column list so it can't silently go stale as `players` grows more columns |
| priority_conflict_dismissals | no | all ops +site | (via officer) | Officer-acknowledged Priority List same-boss conflicts, one row per (player, season, boss, track) an officer has dismissed -- `buildPriorityConflictsBannerHtml()` (js/tabs/tab-priority.js) filters these out of the banner. `player_id` nullable (`ON DELETE SET NULL`) so an archived/removed player doesn't block the dismissal row from existing; team resolved directly from the row's own `team_id`, checked against `player_id`'s team by the same `check_team_id_matches_player()` trigger `self_received_requests` uses |
| priority_order | yes | all ops +site | (via officer) | View-only for a guild officer is enforced purely at the frontend (edit/regenerate controls hidden) -- the table is already public-read, and the write policy deliberately does NOT get `is_guild_officer()` ([#607](https://github.com/katogaming88/WGA-Raid-Hub/issues/607)). `is_site_admin()` is OR'd in, no such carve-out for site admins anywhere in the schema |
| priority_order_confirmed_empty | yes | all ops +site | (via officer) | Marks a team/season/item/track priority list as deliberately saved with nobody ranked -- one row per such item/track, upserted/cleared by `save_priority_order()` itself, not written directly by the frontend. Lets `mapSupabasePriorityOrder()` (js/common.js) seed an empty-but-present `heroic`/`mythic` array so a legitimately-empty item stays out of Unmanaged Items instead of reappearing on reload |
| priority_stale_dismissals | no | all ops +site | (via officer) | Officer-acknowledged Priority List "stale-after-Heroic" conflicts (a Mythic #1 who already has the Heroic version of the same item), one row per (player, season, item) an officer has dismissed -- sibling to `priority_conflict_dismissals`, same shape (nullable `player_id` via `ON DELETE SET NULL`, `check_team_id_matches_player()` trigger), just keyed by item instead of boss/track |
| raid_encounters | yes | | | Read-only lookup; no write policy ([#285](https://github.com/katogaming88/WGA-Raid-Hub/issues/285)) |
| raid_rsvp_reminders_sent | no | | | Locked to `claude_readers` and the service role only (#895, part of #640) -- pure dedup log for the optional-night 24h/2h DM reminder sweep, written solely by the `optional-rsvp-reminders` Edge Function. No officer or site-admin read policy: unlike `audit_log`, this has no legitimate end-user or officer read use case (checkpoints are meaningless without cross-referencing `raid_rsvps`/`raid_schedule`, and a service-role SQL Editor query is enough for debugging) |
| raid_rsvps | no | SELECT +site | | +guild ([#607](https://github.com/katogaming88/WGA-Raid-Hub/issues/607)), read-only. No public read (a raider's stated reason for missing a raid is not guild-wide data). No INSERT/UPDATE/DELETE policy for anyone, officers included -- `set_own_rsvp()` (SECURITY DEFINER) is the only write path (#893, part of #640), deriving the caller's own `player_id` from `auth.uid()` itself rather than trusting a parameter. A raider also reads their own rows via `is_own_player(player_id)` |
| raid_schedule | yes | all ops +site | (via officer) | +guild: `is_guild_officer()` OR'd into the write policy, same shape as `attendance`. The calendar's recurring weekly rule (#892, part of #640). No raider self-write; Phase 3 (#894) adds the admin UI for this policy to actually be used from |
| raid_schedule_exceptions | yes | all ops +site | (via officer) | +guild, same shape as `raid_schedule` above. One-off cancel/add on top of `raid_schedule`'s recurring rule (#892) |
| raid_signup_sheets | no | | | Locked to `claude_readers` and the service role only (#900, part of #640) -- bookkeeping for the bot-owned aggregated Discord signup sheet (which channel/message holds the one edited-in-place embed per team/raid_date), written and read only via `claim_raid_signup_sheet()` from the bot's own service-role Supabase client. Same no-officer-read shape as `raid_rsvp_reminders_sent` -- no end-user read use case |
| raid_zones | yes | | | Read-only lookup; no write policy ([#285](https://github.com/katogaming88/WGA-Raid-Hub/issues/285)) |
| rclc_loot | yes | all ops +site | (via officer) | |
| scoring | yes | all ops +site | (via officer) | Team resolved through `players.team_id` subquery |
| season_signups | no | SELECT +site, UPDATE +site | | No table INSERT policy; `submit_season_signup()` (SECURITY DEFINER) is the only write path ([#403](https://github.com/katogaming88/WGA-Raid-Hub/issues/403)). A raider reads and edits their own signup only through `get_own_signup()`/`update_own_signup()` (both SECURITY DEFINER, [#500](https://github.com/katogaming88/WGA-Raid-Hub/issues/500)) -- no read or write rule exists on the table itself for a raider role |
| self_received_requests | no | SELECT +site, UPDATE +site | | No table INSERT or DELETE policy; `submit_self_received()`/`direct_mark_received()` (both SECURITY DEFINER) are the only insert paths ([#406](https://github.com/katogaming88/WGA-Raid-Hub/issues/406)) and `delete_self_received_request()` is the only delete path ([#756](https://github.com/katogaming88/WGA-Raid-Hub/issues/756)). A raider also reads their own rows (any status) via `is_own_player(player_id)` -- without this, a raider's own auto-approved or officer-approved submission was invisible to them even though it was already `approved`, since the only prior SELECT rules were officer/team_leader/site_admin |
| site_admins | no | | | Site admins only: SELECT and all ops via `is_site_admin()` |
| site_settings | yes | | | Singleton row (id=1); no write policy, `admin_set_maintenance_mode()` (SECURITY DEFINER) is the only write path ([#245](https://github.com/katogaming88/WGA-Raid-Hub/issues/245)) |
| streamers | yes | all ops +site | (via officer) | First self-service write policy in the schema ([#286](https://github.com/katogaming88/WGA-Raid-Hub/issues/286)): a raider can also INSERT/UPDATE/DELETE their own row via `is_own_player(player_id)`, independent of the officer/team-leader grant |
| team_discord_config | no | | | Locked to `claude_readers` and the service role only (#991) -- per-team Discord infra (guild/channel/role ids, script URLs) for the consolidated multi-tenant bot, written and read only by the bot's own service-role Supabase client. Same no-officer-read shape as `raid_signup_sheets`/`raid_rsvp_reminders_sent` -- no end-user read use case, and no admin UI writes it yet |
| team_members | no | SELECT +site | all ops +site | +guild ([#607](https://github.com/katogaming88/WGA-Raid-Hub/issues/607)) on SELECT only, not the separate team-leader write policy. Members also read their own row (`auth_user_id = auth.uid()`) |
| team_raid_progress | yes | all ops +site | (via officer) | Written mainly by the `wcl-progression-sync` Edge Function's service-role cron ([#285](https://github.com/katogaming88/WGA-Raid-Hub/issues/285)); the officer/team-leader policy exists so a bad sync row can be corrected by hand |
| team_settings | yes | | all ops +site | |
| teams | yes | | | Read-only lookup; no write policy |
| tier_token_map | yes | | | Read-only lookup ([Midnight Season 2 tier token -> resolved class item map](https://github.com/katogaming88/WGA-Raid-Hub/pull/650)); no write policy |

## Views and functions

None of these carries a policy of its own. The view and the `SECURITY INVOKER` function defer to the table policies above; the `SECURITY DEFINER` function deliberately bypasses them for one specific, validated operation.

- **`pending_roster`** (view): the officer worklist of approved signups awaiting a roster add (`season_signups` where `status = 'approved'` and `approved_player_id is null`, joined to `classes_specs`). Created `WITH (security_invoker = on)`, so it runs with the caller's privileges and the `season_signups` policies apply to whoever queries it: anon and raiders see zero rows, officers and team leaders see their own team. SELECT is granted to `anon` and `authenticated`; the grant is safe because the underlying policies do the filtering.
- **`incoming_roster`** (view): a raider-facing preview of approved signups awaiting a roster add for the team's currently open signup season (#499), joined to `classes_specs` for class/spec/role and to `team_settings` to scope by `config->>'activeSignupSeason'`. Deliberately NOT created `WITH (security_invoker = on)` -- unlike `pending_roster`, this view is meant to surface these rows to anon and raiders, so it runs under the view owner's own reach into `season_signups` rather than deferring to that table's officer-only read rule; the column list is the actual safety boundary (display name, class, spec, role, team_id only -- no applicant note, officer note, reviewer, or timestamp column ever appears). SELECT is granted to `anon` and `authenticated`.
- **`add_signup_to_roster(signup_id, is_trial, archive_player_id, is_backup_tank, is_backup_healer)`** (function): atomically promotes an approved signup to the roster (creates or unarchives the `players` row, optionally archives a main-swap predecessor on the same team, sets `status = 'added'` and `approved_player_id`). `is_backup_tank`/`is_backup_healer` are decided at promotion time the same way `is_trial` is -- there's no column for either on `season_signups` itself. `SECURITY INVOKER`, so authorization comes entirely from the caller passing the existing `players` write and `season_signups` update policies. EXECUTE is granted to `authenticated` only and revoked from `anon` and `public` (re-granted explicitly since adding these two trailing parameters produced a distinct function rather than replacing the original in place, and a brand new function doesn't inherit the original's grants).
- **`claim_character(team_id, name_realm)`** (function): links a raider's chosen character to their person row, setting `players.team_member_id` and creating the `team_members` row (`role = 'raider'`) on a first claim, or reusing an unlinked row imported from the Discord Claims sheet ([#338](https://github.com/katogaming88/WGA-Raid-Hub/issues/338)). `SECURITY DEFINER` because a claiming raider has no role yet and cannot pass the `players` or `team_members` write policies; it validates that the character is an unarchived, unclaimed roster member of the team and derives the caller's Discord id from `auth.users` rather than trusting a parameter. EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`.
- **`submit_self_received(team_id, name_realm, item_name, track, source, note, slot)`** (function): a raider's self-received loot submission, inserting `pending` unless the caller is authenticated as the named character (`players.team_member_id -> team_members.auth_user_id = auth.uid()`) and the source is not `Other`, in which case it inserts `approved` immediately and writes the `Self-Received Auto-Approved` audit entry itself ([#406](https://github.com/katogaming88/WGA-Raid-Hub/issues/406); auto-approve narrowed and audited by the two `20260819*` migrations). `slot` carries the raw `bis_items.slot` the approval sync targets (#386). Rejects outright (no insert at all) when `note` mentions "raid" as its own word -- actual raid drops are recorded by the officer's RCLootCouncil import instead, and raiders kept working around the lack of a "Raid Drop" source by describing one under `Other` (`20260828125630`). `SECURITY DEFINER` because `self_received_requests` has no INSERT policy for anyone. EXECUTE is granted to `anon` and `authenticated`.
- **`direct_mark_received(team_id, name_realm, item_name, track, source, note, slot)`** (function): an officer marking an item received on a raider's behalf, bypassing the approval queue -- inserts `approved` directly ([#406](https://github.com/katogaming88/WGA-Raid-Hub/issues/406)). `SECURITY DEFINER`, checking `my_team_role()`/`is_site_admin()` in the function body since (like `submit_self_received`) there's no INSERT policy to lean on. EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`.
- **`delete_self_received_request(id)`** (function): an officer deleting a single request row of any status, the correction path for duplicates and mistaken decisions ([#756](https://github.com/katogaming88/WGA-Raid-Hub/issues/756)). Locks the row, raises on not-found, and derives authorization from the row's own team (`my_team_role()` in officer/team_leader wrapped in coalesce, or `is_site_admin()`; guild officers deliberately excluded). Writes the `Self-Received Deleted` audit entry inside the function, targeting `players` with a summary-string detail, since the request row is gone afterwards. Deliberately does not touch `bis_items.obtained` (the one-way sync decision). `SECURITY DEFINER` because the table has no DELETE policy for anyone. EXECUTE is granted to `authenticated` only and revoked from `public`.
- **`is_boe_manager()`** (function): true when `auth.uid()` appears in `boe_managers`, same shape as `is_site_admin()` and `is_guild_officer()`. Guild-wide with no team parameter ([#766](https://github.com/katogaming88/WGA-Raid-Hub/issues/766), reshaping the per-team version #745 shipped): BoEs are guild property, so a grantee is authorized on every team's finds regardless of which team they raid on, and the grant persists until a site admin revokes it rather than self-revoking on demotion. `SECURITY DEFINER`; EXECUTE is granted to `anon` and `authenticated` (policies referencing it are evaluated for anon, which would otherwise error rather than resolve false, same as `is_guild_officer()`).
- **`is_any_team_officer()`** (function): true when the caller holds `officer` or `team_leader` on any team ([#766](https://github.com/katogaming88/WGA-Raid-Hub/issues/766)). Exists only to gate the `boe_managers` read, which has no `team_id` to scope by since the grant went guild-wide. `SECURITY DEFINER` for the usual reason a policy helper is: a policy reading `team_members` inline would be evaluated under the caller's own row security on that table, making its correctness depend on an unrelated policy. EXECUTE is granted to `anon` and `authenticated`.
- **`can_settle_boe(team_id)`** (function, [#888](https://github.com/katogaming88/WGA-Raid-Hub/issues/888)): true for a BoE manager, a site admin, or an `officer`/`team_leader` on that team via `my_team_role(team_id)`, the same authority the `boe_items` read policy already trusts. Gates only `boe_mark_paid` and the paid-to-sold edge of `boe_revert`: the officers who hand out the gold settle it and can undo that one step, nothing else. Native by role rather than a per-team switch or a per-team grant list, so an officer of a team with no page (Immolation, Wrathless) is one `team_members` row. `SECURITY DEFINER`; EXECUTE is granted to `anon` and `authenticated` like `is_boe_manager()`.
- **`current_discord_id()`** (function, [#889](https://github.com/katogaming88/WGA-Raid-Hub/issues/889)): the caller's Discord id from `auth.users.raw_user_meta_data ->> 'provider_id'`, the same claim `claim_character()` and the admin grant trios read and the string `link_auth_user_to_member()` matches against `team_members.discord_id`; null for anon and for an account with no provider id. Exists for the raider read on `boe_items` and `boe_listings`. `SECURITY DEFINER` because it reads `auth.users`; EXECUTE is granted to `anon` and `authenticated` like `is_boe_manager()`, since the policies referencing it are evaluated for anon.
- **`admin_grant_boe_manager(discord_id)` / `admin_list_boe_managers()` / `admin_revoke_boe_manager(discord_id)`** (functions, [#766](https://github.com/katogaming88/WGA-Raid-Hub/issues/766)): the only write path onto `boe_managers`, mirroring the guild-officer and site-admin trios exactly. All three `SECURITY DEFINER` and gated on `is_site_admin()`: handing someone the guild bank is an admin action, not something managers do to each other. Grant resolves `auth_user_id` from `auth.users.raw_user_meta_data ->> 'provider_id'` and refuses a duplicate; revoke raises when the account holds no grant; both audit-log. A null `auth_user_id` in the list means the grant has not activated because that Discord account has never signed in; `link_auth_user_to_member()` fills it on their first login. EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`.
- **`submit_boe_found(team_id, name_realm, item_name, track, note, donate, upgrade_rank)`** (function): the raider-facing found-BoE submission ([#745](https://github.com/katogaming88/WGA-Raid-Hub/issues/745)), anon-callable for parity with the Google Form it replaces. `SECURITY DEFINER` because `boe_items` has no INSERT policy for anyone. Stamps `finder_discord_id` on the row from `current_discord_id()`, null for a signed-out submit and never client-supplied ([#889](https://github.com/katogaming88/WGA-Raid-Hub/issues/889)); the transition trigger keeps that column off the plain-UPDATE list. Resolution is non-fatal, unlike `submit_self_received`: an unrostered finder keeps the raw `finder_name` with `player_id` null, and a name outside the BoE catalog keeps `item_id` null, since a found BoE is a fact, not a request ([#875](https://github.com/katogaming88/WGA-Raid-Hub/issues/875): only a row flagged `is_boe` links, case-insensitively, and the catalog spelling is stored). `donate` records the finder's intent to give up their cut ([#862](https://github.com/katogaming88/WGA-Raid-Hub/issues/862)). `track` and `upgrade_rank` are required and the rank must be one of 1/6 to 6/6 ([#865](https://github.com/katogaming88/WGA-Raid-Hub/issues/865)); the raider form checks both first, and the raises catch a stale client. EXECUTE is granted to `anon` and `authenticated`.
- **`boe_record_listing(id, price, listed_at, note)` / `boe_record_sale(id, sale_price, sold_at)` / `boe_mark_paid(id, paid_at, donated)` / `boe_retire(id, note)` / `boe_revert(id)`** (functions, [#745](https://github.com/katogaming88/WGA-Raid-Hub/issues/745)): the BoE lifecycle. Each locks the `boe_items` row (`select ... for update`), gates in the function body, then validates the from-status and raises on an illegal edge. The gate is `is_boe_manager() or is_site_admin()` (a `boe_managers` grant, never plain officer role; guild-wide since [#766](https://github.com/katogaming88/WGA-Raid-Hub/issues/766)) for listing, sale, retire and every revert edge but one; `boe_mark_paid` and the paid-to-sold edge of `boe_revert` gate on `can_settle_boe(team_id)` instead, so a team's own officers settle the payouts they hand out and can undo that one step ([#888](https://github.com/katogaming88/WGA-Raid-Hub/issues/888)). `boe_record_sale` computes the split from the `site_settings` floor/pivot constants: the game's fixed 5% auction house fee comes off the top, the finder gets 20%-of-gross or the floor, whichever is larger, capped at the sale net of the fee, and the guild keeps the rest ([#861](https://github.com/katogaming88/WGA-Raid-Hub/issues/861)); it snapshots both constants and the fee onto the row and returns all four figures, and `finder_payout + guild_cut + ah_fee = sale_price` holds on every sold or paid row by constraint. `boe_mark_paid` sets `payout_donated` from its third argument, the only way that flag is written ([#862](https://github.com/katogaming88/WGA-Raid-Hub/issues/862)); the trigger blocks it on a plain UPDATE. `boe_revert` walks the correction edges (paid to sold leaving the flag, sold to listed-or-found nulling the money, retired to found). All `SECURITY DEFINER`; EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`.
- **`set_boe_payout_settings(floor, pivot)`** (function): the only write path onto the guild-wide `site_settings.boe_payout_floor`/`boe_payout_pivot` payout constants ([#745](https://github.com/katogaming88/WGA-Raid-Hub/issues/745)). `SECURITY DEFINER`, gated on `is_site_admin()` only (money policy, narrower than the bios setter's site-or-guild-officer gate), validates the bounds, and audit-logs the change. EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`.
- **`get_own_signup(team_id)`** (function): a raider's own most recent season signup for the team's currently active signup season, joined to `classes_specs` for both the main and swap class/spec. `SECURITY DEFINER` because `season_signups` has no read rule for a raider at all; returns a fixed column list that deliberately omits `signup_officer_note` and `reviewed_by` so no future frontend query can accidentally request them ([#500](https://github.com/katogaming88/WGA-Raid-Hub/issues/500), same officer-eyes-only rule as #499). Returns no rows for a signed-out caller or one with no matching row. EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`.
- **`update_own_signup(signup_id, name_realm, class, spec, off_specs, main_swap, player_note, swap_from_name_realm)`** (function): lets a raider correct their own signup (typo, realm transfer, mislabeled main swap) while it is still `pending`, while `approved` and not yet promoted, or while already `added` to the roster **if** the row's `season` is still the team's active signup season (`get_own_signup()` only ever returns an `added` row in that state to begin with, so reaching the edit form already implies signups are open) -- in all three cases the edit reverts `status` to `pending`, clears `reviewed_at`/`reviewed_by`/`signup_officer_note`, and (for the `added` case) nulls `approved_player_id` to satisfy `season_signups_player_only_when_added`. It deliberately does **not** touch the `players` row it once promoted -- an edited signup goes back through officer review and a fresh `add_signup_to_roster()` promotion rather than writing the live roster unreviewed. An `added` signup whose season has moved on, or a `rejected` signup, cannot be edited through this function. `SECURITY DEFINER`; the ownership check rides on the `UPDATE`'s own `WHERE auth_user_id = auth.uid()` clause (same TOCTOU-safe shape as `claim_character()`) rather than a separate prior read. EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`.
- **`notify_player(player_id, message)`** (function): writes an in-app notification row for a raider, called from the officer-side approve/reject handlers for BiS link requests, self-received items, and M+ exclusion requests ([#151](https://github.com/katogaming88/WGA-Raid-Hub/issues/151)). Deliberately not wired to season signup approve/reject: an applicant has no `players` row (and so no way to resolve a notification target) until their signup is promoted to the roster, well after that step. `SECURITY DEFINER`, resolving `team_id` from `player_id` and checking `my_team_role()`/`is_site_admin()` in the function body -- same shape as `write_audit_log()`, and deliberately the only insert path onto `notifications` so a notification can't be forged or misattributed. EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`.
- **`write_audit_log(team_id, action, target_type, target_id, detail)`** (function): the only INSERT path onto `audit_log` ([#214](https://github.com/katogaming88/WGA-Raid-Hub/issues/214)); every officer write feature calls it after the fact to self-log. `SECURITY DEFINER`, gated on `my_team_role(team_id)` officer/team_leader, `is_site_admin()`, `is_guild_officer()` ([#607](https://github.com/katogaming88/WGA-Raid-Hub/issues/607) -- added so a guild officer's player/attendance/bio writes, which now pass the underlying table policy, don't then fail at the audit-log step), or `is_boe_manager()` ([#766](https://github.com/katogaming88/WGA-Raid-Hub/issues/766) -- same reason once the BoE grant stopped requiring an officer role; `js/common.js` only `console.warn`s on failure here, so without it a manager with no officer role would move money and log nothing). Both ORs widen the function beyond the writes that motivated them: either grant can now log any action on any team. EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`.
- **`set_guild_officer_bios(bios)`** (function): the only write path onto `site_settings.guild_officer_bios`, the guild-wide officer bio cards ([#577](https://github.com/katogaming88/WGA-Raid-Hub/issues/577), moved to `site_settings` in [the 2026-07-26 decision](database-decisions.md)). `SECURITY DEFINER`, gated on `is_site_admin() or is_guild_officer()` ([#607](https://github.com/katogaming88/WGA-Raid-Hub/issues/607) -- a deliberate reversal of that decision's site-admin-only reasoning, since a guild officer represents the same kind of cross-team authority a site admin does). EXECUTE is granted to `authenticated` only.
- **`set_team_officer_bios(team_id, bios)`** (function): the write path onto the per-team `team_settings.config -> 'teamOfficerBios'` bio cards, alongside (not replacing) the generic `set_team_setting()` for that one key. `team_settings`' own write policy (`all ops +site` in the matrix above) is team-leader-only, but this bio editor was always meant to be officer-facing -- the Officer Bios tab never gated the Save button to team leaders, so a plain officer hit "Not authorized" through the generic path. `SECURITY DEFINER`, gated on `my_team_role(team_id)` officer/team_leader, `is_site_admin()`, or `is_guild_officer()` -- same admission set as the `players`/`attendance` officer-write policies. EXECUTE is granted to `authenticated` only.
- **`is_optional_raid_night(team_id, raid_date)`** (function, #895, part of #640): whether a given `raid_date` is an optional raid night for a team, per `raid_schedule`/`raid_schedule_exceptions` (a cancelled exception wins over the recurring rule, an added exception wins over the recurring rule, else the active recurring rule for that weekday, else not a raid night). `STABLE SQL`, not `SECURITY DEFINER` (reads only, no elevated writes). Ports `js/calendar.js`'s `computeRaidNights()` precedence into SQL so `set_own_rsvp()`'s server-side gate and the `optional-rsvp-reminders` Edge Function agree on the same definition rather than maintaining it twice. EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`.
- **`set_own_rsvp(team_id, raid_date, status, note)`** (function): a raider's self-mark RSVP override for one raid night (#893, part of #640). `SECURITY DEFINER`; unlike `update_own_signup()`'s TOCTOU-safe write-then-check-`WHERE` shape, this derives the caller's own `player_id` fresh from `auth.uid()` via the `players`/`team_members` join (same path `is_own_player()` uses) inside the same statement it writes with, so there is no client-supplied id to race against. Raises if the caller has no active roster character on that team. Benched players (`players.is_bench`) are blocked entirely on a normal night (nothing to override there) but, as of #895, may call this on an **optional** night (`is_optional_raid_night()` true) with any status -- an optional night has no default for anyone, bench included, and bench is exactly who might get pulled in for one. A `null` status deletes the row instead of raising, reverting to the computed default. `status` accepts `Attending`/`Late`/`Leaving Early`/`Tentative`/`Absent`; `Attending` itself is rejected unless the target night is optional, regardless of bench status (#895). EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`.
- **`raid_night_info(team_id, raid_date)`** (function, #900, part of #640): sibling to `is_optional_raid_night()` -- same three-tier `raid_schedule`/`raid_schedule_exceptions` precedence, but also surfaces `start_time`/`timezone`/existence, not just an optional-night boolean. Used by the bot's own service-role client (`wga-raid-bot/src/signupSheet.ts`) to build the signup-sheet embed's header and to scan upcoming nights for its proactive lead-time sweep. `plpgsql`, `STABLE`, not `SECURITY DEFINER`. No explicit `revoke` from `anon`/`public` (matches `wishlist_setup_status()`'s precedent, #8 wga-raid-bot) -- harmless since it only reads, and RLS on the underlying tables still applies to any non-service-role caller. `grant execute ... to service_role`.
- **`claim_raid_signup_sheet(team_id, raid_date, channel_id)`** (function, #900, part of #640): atomic get-or-create for `raid_signup_sheets` -- row-locks the tracking row (`select ... for update`) so the bot's service-role client knows whether to CREATE (returns null `message_id`) or EDIT (returns the stored id) the aggregated signup-sheet embed, and resets `message_id` to null if the configured channel changed since the row was created. `plpgsql`, not `SECURITY DEFINER` (the underlying table has no policy for anyone but the service role, so RLS itself is the gate). `grant execute ... to service_role`, same no-revoke shape as `raid_night_info` above.
- **`admin_list_guild_officers()` / `admin_grant_guild_officer(discord_id)` / `admin_revoke_guild_officer(discord_id)`** (functions, [#607](https://github.com/katogaming88/WGA-Raid-Hub/issues/607)): grant/revoke/list for the `guild_officers` table, mirroring `admin_{list,grant,revoke}_site_admin()` exactly (same Discord-ID resolution, same shape). All three are gated on `is_site_admin()` only -- granting this tier is itself an admin action, not something a guild officer can do to themselves or each other. `SECURITY DEFINER`. EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`. Until [#910](https://github.com/katogaming88/WGA-Raid-Hub/issues/910) this was the one grant table `link_auth_user_to_member()` did not cover, so a guild officer granted before their first sign-in stayed unlinked; both current holders happen to have signed in first, which is why nobody hit it.
- **`admin_grant_team_role(team_id, discord_id, role)` / `admin_revoke_team_role(team_id, discord_id)`** (functions, [#910](https://github.com/katogaming88/WGA-Raid-Hub/issues/910)): the grant path for the per-team tier, which had none. Before this, a `team_members` row for a normal user could only come from `claim_character()`, which needs a claimable character on that team, so a team with no roster could not be given an officer at all. Both are `SECURITY DEFINER` and gated on `is_site_admin() or my_team_role(team_id) = 'team_leader'`; because `my_team_role()` is null for everyone on a team with no members, opening a rosterless team is a site-admin action by construction. Grant resolves `auth_user_id` from `auth.users.raw_user_meta_data ->> 'provider_id'` at grant time and returns it, so an inert grant is visible immediately. It **refuses to change a role that is already set** (`role` drives `my_team_role()`, `can_settle_boe()`, `is_any_team_officer()`, `is_team_leader_anywhere()` and the read rules built on them, so a silent rewrite by a mistyped Discord id would demote a sitting team leader with the audit entry reading like a fresh grant); the one repeat case that writes is filling an `auth_user_id` that was never linked. Revoke **demotes to `raider` whenever a `players` row points at the member** and deletes only a memberless row, because `players_team_member_id_fkey` is `ON DELETE SET NULL` and a delete would silently unclaim that character. Both audit-log, and the demote and the delete use different actions. EXECUTE is granted to `authenticated` only and revoked from `anon` and `public`.
- **`resolve_boe_finder_discord_id(boe_id)`** (function, [#918](https://github.com/katogaming88/WGA-Raid-Hub/issues/918)): the finder of a BoE row as a Discord id, for the sold ping. Three steps in order: the id stamped on the row at submit ([#889](https://github.com/katogaming88/WGA-Raid-Hub/issues/889)), else the claimed character's `team_members` row, else a guild-wide first-name match over `players.name_realm` that includes removed characters, because a find can be reported months after its character left a roster. `SECURITY DEFINER` because that walk crosses `players` and `team_members`, whose self-read returns only the caller's own row, so a BoE manager holding no officer role anywhere could not do it for themselves. Gated on `is_boe_manager()` or `is_site_admin()`, the same pair `boe_record_sale` requires, so it admits exactly the people who could have caused the message it feeds. Returns null when nothing matches, and also when one name reaches two different Discord ids: `boe-sold-webhook` then renders the finder's name in bold with no ping rather than pinging a guess.

## Known issues

- `service_role` is missing base DML grants on every table, the same defect [#284](https://github.com/katogaming88/WGA-Raid-Hub/issues/284) fixed for `anon` and `authenticated`. service_role bypasses policies but not grants, so server-side writes (Edge Functions, service-key integrations) will fail until it gets the same treatment. Flagged on #284.

## Verifying against a live database

The raw, ungrouped policy list (one row per policy, straight from `pg_policies`) is committed as [rls_policies.csv](rls_policies.csv), which opens in any spreadsheet tool for sorting and filtering. Regenerate it with `npm run db:rls` (stack running); CI fails a PR when the committed CSV no longer matches what the migrations produce. For interactive browsing, local Supabase Studio (http://127.0.0.1:54323) has a Policies page showing each table's policies with their SQL.

The same list, ad hoc in psql:

```sql
select tablename, policyname, roles, cmd, qual, with_check
from pg_policies
where schemaname = 'public'
order by tablename, policyname;
```

The vitest RLS policy-matrix harness in [`tests/rls/`](../tests/rls/) exercises these policies with impersonated anon, raider, officer, team leader, and site admin sessions and is the executable form of this document. Run it with `supabase db reset` then `npm run test:rls`.
